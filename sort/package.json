{
  "name": "sort",
  "version": "1.0.0",
  "description": "to generate tsconfig file we need to type command",
  "main": "index.js",
  "scripts": {
    "start:build": "tsc -w",
    "start:run": "nodemon build/index.js",
    "start": "concurrently npm:start:*"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^5.1.0",
    "nodemon": "^2.0.3"
  }
}
